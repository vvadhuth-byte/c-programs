#include <stdio.h>
int main() {
    int a,b,c;
    scanf("%d",&a);
    if(a>=0&&a<=100){
    printf("bill amount = Rs.%d",b);
    }
    else if(a>=101&&a<=500)
    printf("bill amount = Rs.%d",c);
    return 0;
}